
{
  "name": "stablecoin-hardhat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}
